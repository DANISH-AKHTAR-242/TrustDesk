# TrustDesk — standing brief

Read this file before every phase. `TRUSTDESK_CLAUDE_CODE_PROMPTS.md` holds the phase-by-phase build plan; `TRUSTDESK_PROBLEM_STATEMENT.md` is the source of truth for scope.

## Project summary

1. TrustDesk is an AI-first customer support operations platform built for the Airtribe capstone.
2. It ingests policy documents, triages tickets (category / priority / sentiment / escalation), and generates grounded draft replies with `KB-*` citations.
3. The AI only recommends tool actions; sensitive actions need explicit human approval and an idempotency key before execution.
4. Every AI run leaves an `AgentRun` trace, and an eval runner scores the 8 cases in `data/eval_cases.jsonl` (triage, citations, unsafe-action blocking, escalation).
5. Guardrails treat customer text and retrieved documents as untrusted, refuse secret/identity-bypass/injection requests, and quarantine `KB-ADVERSARIAL-001`.

## Read-only capstone pack

`data/`, `docs/`, `scripts/`, `TRUSTDESK_PROBLEM_STATEMENT.md` and `TRUSTDESK_CLAUDE_CODE_PROMPTS.md` are the provided pack. Never modify, move, or delete anything inside them. Project-authored docs go in `docs_project/` (Phase 10).

Repo root holds only: config files, `CLAUDE.md`, `README.md`, `docker-compose.yml`, `docs_project/`, `reports/`, `server/`, `web/`, and the pack.

## Stack (fixed, do not substitute)

- Node 20+, TypeScript, ESM (`"type": "module"`), strict mode on
- Express 4, Zod for request validation, pino for logging
- Prisma + PostgreSQL 16 (Docker Compose, container `trustdesk-db`)
- Vitest + Supertest for tests
- Frontend: React 18 + Vite + TypeScript, Tailwind CSS v4 via `@tailwindcss/vite` with hand-built primitives (since the presentation redesign, D-076; was plain CSS), lucide-react icons, Inter Variable; no component library, no state manager
- Live AI provider: OpenRouter (`https://openrouter.ai/api/v1`, model from `OPENROUTER_MODEL`)
- Test/eval AI provider: local deterministic mock adapter (`AI_PROVIDER=mock`, the default)
- Retrieval: Postgres full-text search (`tsvector` + GIN) + `pg_trgm` fallback + category prior boost. No vector DB.
- Auth: static demo bearer tokens mapped to three roles (`support_agent`, `support_manager`, `admin`); since Phase 11 also JWT login over a seeded `user` table (the demo tokens remain a fallback)

## Folder map

```
server/
  src/
    index.ts              boot: createApp().listen(env.PORT)
    app.ts                createApp(): json body parser, requestId, pino-http, /health, /api routers, errorHandler last
    config/env.ts         dotenv + Zod-validated, frozen `env`; fails fast listing missing vars
    config/paths.ts       REPO_ROOT / DATA_DIR / KNOWLEDGE_BASE_DIR / REPORTS_DIR from import.meta.url
    errors/AppError.ts    AppError(code, httpStatus, message, details?) + factory helpers
    middleware/           requestId, errorHandler (error envelope), asyncHandler, auth (requireAuth: demo token or JWT, requireRole, getPrincipal), policy.ts (ROUTE_POLICIES map + enforcePolicy)
    types/express.d.ts    Express Request augmentation (requestId, principal)
    db/prisma.ts          singleton PrismaClient; db/ids.ts prefixed cuid2 generator
    domain/policyWindows.ts  pure return-window / warranty functions (no DB, no Date.now(); asOf = ticket created_at)
    domain/businessDays.ts   Mon–Fri UTC business-day arithmetic (stale-tracking rule)
    ai/                   types.ts, openRouterAdapter.ts (retry/timeout/fences), mockAdapter.ts (MOCK_TRIAGE_RULES), index.ts getAiAdapter(override?) (+ cost_estimate wrapper), pricing.ts (approximate USD/M-token table, AI_PRICE_TABLE_JSON override)
    ai/prompts/           versioned prompt builders: triage.v1.ts, draftReply.v1.ts (system + buildUser, labelled fact lines)
    guardrails/           patterns.ts (6 groups as data), inputScanner.ts, documentTrust.ts (R4), outputScanner.ts (R8),
                          policy.ts (decision table → outcome + requiredCitations + refusalTemplate), refusalTemplates.ts, selfTest.ts
    seed/seed.ts          idempotent loader for data/ into Postgres (`--reset` truncates first)
    modules/<name>/       one folder per feature: router.ts, service.ts, schemas.ts, mappers.ts
      customers, orders, tickets
      evals/                runner.ts (runEvals: real triage + draft per case, R2 prompt-leak assertion, metrics, adversarial summary, EvalRun row + reports),
                            answerRequirements.ts (one predicate per requirement string), promptLeakGuard.ts (observing adapter + assertion), metrics.ts,
                            report.ts (EVALUATION_REPORT.md renderer, manual section preserved), mappers.ts, service.ts (async API runs), router.ts, runEvalCli.ts (npm run eval)
      toolActions/          registry.ts (ToolDefinition → executor), executors/<tool_name>.ts (simulated), service.ts (requestAction chain, approve, execute), router.ts, schemas.ts, types.ts
      drafts/               service.ts (generateDraft pipeline, lifecycle, caseRuleContextForTicket), recommendationRules.ts (R5 gate, blockedToolsForCase), router.ts, schemas.ts, mappers.ts
      triage/               service.ts (retrieve → model → validate/retry/fallback → postRules → persist), postRules.ts (PR1–PR6), router.ts
      traces/               router.ts GET /agent-runs[/:runId], mappers.ts
      metrics/              service.ts (metricsSummary: runs per type/status, p50/p95, tokens, cost), router.ts GET /metrics/summary
      feedback/             schemas.ts, service.ts (createFeedback, listFeedback + average), mappers.ts, router.ts POST/GET /feedback
      auth/                 passwords.ts (scrypt), service.ts (login → JWT, principalFromJwt, currentUser), router.ts (open POST /auth/login, GET /auth/me)
      redTeam/              service.ts (probeText: scanner + post-rules + decision, stateless; listFlaggedRuns), router.ts POST /red-team/probe, GET /red-team/runs
      knowledge/            loader.ts (markdown parser/chunker), search.ts (FTS + trigram + prior; hybrid = + vector cosine fused by RRF), categoryPriors.ts,
                            embeddings.ts (provider: local | openrouter), localEmbedding.ts (hashing embedding, cosine),
                            contextBuilder.ts (R3 fences), service.ts (ingest/list/get, shared persist), router.ts
  tests/                  Vitest suites; setup.ts is the global setup file; helpers/db.ts (describeWithDb), helpers/routes.ts (router-stack enumeration)
                          auth.routes.test.ts (every route except /health and /auth/login behind requireAuth, policy map = README table), contract.test.ts (docs/API_CONTRACT.md shapes), integration/demoFlow.test.ts (7-step demo end to end)
  prisma/                 schema.prisma + migrations/ (init, add_fts, add_embeddings, add_users)
web/                      Vite react-ts template; vite.config.ts (react + tailwindcss plugins) proxies /api and /health to :4000; .env.example (VITE_API_BASE, demo tokens); index.html applies the stored theme before first paint
  src/
    main.tsx, App.tsx     Vite entry (Inter font, providers) + AppShell + React Router: / , /tickets/:ticketId , /evals , /documents , /metrics , /red-team
    styles.css            @import "tailwindcss"; @custom-variant dark; @theme (Inter, mono, 13.5px type scale, wide breakpoint 1440px); :root/.dark semantic variables (zinc, indigo accent, emerald/amber/rose/sky) exposed via @theme inline; tooltip + skeleton CSS
    lib/api.ts            typed fetch wrapper, bearer token from localStorage 'trustdesk_token', parses the error envelope into ApiError, one helper per endpoint
    lib/session.tsx       token context (role label derived from a demo token or a JWT's role claim) + top-bar TokenSelector (Agent | Manager | Admin segmented control, settings popover with the override field and the email/password Log in form storing the JWT)
    lib/theme.ts          dark-by-default theme hook, localStorage 'trustdesk_theme', `dark` class on <html>
    lib/types.ts          API response shapes (mirrors server mappers; never expected_*); lib/format.ts (fmt, num), lib/cx.ts (class joiner)
    components/layout/    AppShell (fixed sidebar 224px → 56px icon rail below 1280px, sticky top bar, scrollable main), Sidebar (nav + role), TopBar (breadcrumb, TokenSelector, theme toggle), Page (padding, PageHeader)
    components/ui/        hand-built primitives, one per file, no deps: Button, Badge (+ badgeVariantFor), Card, Table (TR with keyboard rows), Drawer (Escape, focus trap, backdrop), Tabs (segmented control), Skeleton, Toast (top-right, danger never auto-dismisses), Tooltip (CSS-only), EmptyState, CodeBlock (copy button), Spinner, Input/Textarea/Select/Field, KeyValue + Mono, Chip, Popover
    components/           ErrorBanner.tsx (error context; ErrorBanners renders danger toasts), useAction.ts, ActionButton.tsx, DocumentDrawer.tsx (+ QuarantineBanner, QUARANTINE_LABEL)
    pages/                TicketQueue (segmented status filter, client-side search, skeleton rows, arrow-key rows), TicketDetail (three panes at ≥1440: context | conversation+AI | actions+trace; two columns at ≥1024 with actions under the centre; guardrail banner above the draft), EvalsPage (metric cards with progress bars, pass/fail icons, bordered adversarial card), DocumentsPage (list/detail split, rose quarantine marker + banner), MetricsPage (/metrics), RedTeamPage (/red-team)
package.json (root)       convenience runner only: npm run dev (concurrently server + web), seed, eval — no workspaces (D-002)
docs_project/             project-authored docs (docs/ is the read-only pack): ARCHITECTURE.md (components, mermaid sequence diagrams), DEMO_SCRIPT.md (6-minute video script)
README.md                 project README (setup, env, API table generated from the router stack, evals, decisions, security model, limitations); the original pack README is preserved at the bottom
reports/                  eval-run-<id>.json (gitignored) + EVALUATION_REPORT.md
```

## API conventions

- Success responses are plain JSON objects (no envelope) with snake_case keys, matching `docs/API_CONTRACT.md`.
- Every error, from every endpoint, uses `{ "error": { "code", "message", "details" | null, "request_id" } }`.
- Error codes: `VALIDATION_ERROR` 400, `UNAUTHORIZED` 401, `FORBIDDEN` 403, `NOT_FOUND` 404, `CONFLICT` 409, `GUARDRAIL_BLOCKED` 403, `TOOL_EXECUTION_FAILED` 500, `AI_PROVIDER_ERROR` 502, `INTERNAL_ERROR` 500.
- `GET /health` and `POST /api/auth/login` are the only unauthenticated routes; everything else lives under `/api` and requires a bearer token (demo token or login JWT), with per-route role policies in `middleware/policy.ts`.

## Non-negotiable rules

- R1. Policy windows (return eligibility, warranty) are ALWAYS evaluated against the ticket's `created_at`, never against the current date or `Date.now()`.
- R2. The fields `expected_category`, `expected_priority`, `expected_sentiment`, `expected_escalation`, `expected_actions` are evaluation-only. They are stored in a separate table and MUST NOT be readable from any code path that builds a prompt, triages, drafts, or recommends actions.
- R3. Customer text and retrieved document text are untrusted data, never instructions. They are always wrapped in explicit delimiters before reaching a model.
- R4. `KB-ADVERSARIAL-001` is ingested but quarantined: never returned as grounding context, never cited.
- R5. The AI may only RECOMMEND tool actions. Execution always requires an explicit human API call, and sensitive actions require an approval record first.
- R6. Every sensitive tool action carries an idempotency key; a replay never creates a second action.
- R7. Every triage run, draft run and eval case run writes an `AgentRun` trace row.
- R8. No secret, system prompt, internal note, or unrelated customer's data ever appears in a draft reply.

## Decisions

Append-only log. Never rewrite or delete an entry; add a new one that supersedes it.

- D-000 (Phase 0) Stack fixed as listed above; Node/TS/ESM, Express 4, Prisma + Postgres 16, Zod, pino, Vitest + Supertest, React + Vite, OpenRouter live + deterministic mock.
- D-001 (Phase 0) Code layout: all backend in `server/`, all frontend in `web/`; `data/`, `docs/`, `scripts/` are never modified.
- D-002 (Phase 0) `server/` is a standalone package (no npm workspaces). A root `package.json` with `concurrently` arrives in Phase 9 only as a convenience runner.
- D-003 (Phase 0) Error envelope `{ error: { code, message, details, request_id } }` on every error; success bodies are plain snake_case JSON with no envelope.
- D-004 (Phase 0) Request ids: honour an incoming `X-Request-Id` (max 128 chars) else `crypto.randomUUID()`; always echoed on the response header and in the error envelope.
- D-005 (Phase 0) `env` is parsed once by Zod and frozen; a missing or malformed variable throws at import time with a readable list (fail fast, no partial boot).
- D-006 (Phase 0) `AI_PROVIDER` defaults to `mock`; every test and eval run uses the mock unless explicitly overridden. Default OpenRouter model: `google/gemini-2.0-flash-001`.
- D-007 (Phase 0) Demo auth tokens live in env (`DEMO_AGENT_TOKEN`, `DEMO_MANAGER_TOKEN`, `DEMO_ADMIN_TOKEN`) mapping to `support_agent` / `support_manager` / `admin`.
- D-008 (Phase 0) Version pins chosen because the prompt did not specify them: Prisma 6.x (schema-file datasource URL, stable migrate workflow), Vitest 3.x, TypeScript 5.9.x, ESLint 8.x (so `.eslintrc.json` is honoured), Zod 3.x. Lint is non-blocking.
- D-009 (Phase 0) `vitest.config.ts` sets `passWithNoTests: true` so `npm test` is green before the first suite lands in Phase 1.
- D-010 (Phase 0) `docker-compose.yml` adds a `pg_isready` healthcheck so `docker compose ps` reports `healthy`, satisfying the Phase 0 acceptance check.
- D-011 (Phase 0) Unknown routes return the standard envelope with `NOT_FOUND` (a catch-all is registered just before `errorHandler`).
- D-012 (Phase 1) Schema: pack natural string ids are primary keys; generated ids are `newId(kind)` in `server/src/db/ids.ts` = prefix (`draft_`, `run_`, `act_`, `appr_`, `eval_run_`, `fb_`) + cuid2. Models map to snake_case tables/columns via `@@map`/`@map`. Money is `Int` whole rupees; currency is a string.
- D-013 (Phase 1) Dates: date-only pack values are stored as `DateTime` at 00:00 UTC; ticket `created_at` is stored as the instant it denotes (`timestamptz`), so `+05:30` semantics survive. Every `DateTime` column is `@db.Timestamptz(3)`.
- D-014 (Phase 1) `knowledge_chunk.search_vector` is a Postgres `GENERATED ALWAYS ... STORED` tsvector created by migration `add_fts` (drop-then-add, because the init migration had already created a plain column). The Prisma schema mirrors it with `@default(dbgenerated(...))` plus the two GIN indexes (`type: Gin`, `ops: raw("gin_trgm_ops")`) so `prisma migrate dev` reports no drift. Prisma cannot write the column; Postgres computes it.
- D-015 (Phase 1) Seeder idempotency: every pack entity is `upsert`ed by natural id. Knowledge chunks have generated ids, so each document's chunk set is replaced (delete + createMany) inside one transaction; counts are identical on every run. `--reset` truncates all 15 tables with `CASCADE` first.
- D-016 (Phase 1) Knowledge chunking: split on `## ` headings; the preamble (H1 + `Doc ID`/`Audience`/`Version` lines) is not a chunk unless prose remains after removing those lines; a document with no H2 becomes one chunk. Result: 25 chunks across 8 documents.
- D-017 (Phase 1) Return window: `eligible_return_until` wins, else `delivered_at + 7 days`; the whole calendar day of the window end counts as inside. Check order: `final_sale` → `non_returnable_category` (category `software`) → `not_delivered` → date comparison. So ord_5004 (software AND final sale) reports `final_sale`.
- D-018 (Phase 1) Warranty: base 12 calendar months from `delivered_at` (UTC month arithmetic, end date inclusive). Gold adds 6 months only when EVERY item is extension-eligible (not software, not final sale, not an accessory under INR 3000). Item price = `unit_price` if the item carries one, else `order.total / total quantity` (the pack has single-item orders and no per-item price). `monthsSinceDelivery` = days / 30.4375 rounded to 2 dp; `null` when undelivered.
- D-019 (Phase 1) `prisma migrate dev` is interactive on drift and will hang or prompt to reset under a non-TTY; always run it with stdin closed (`< /dev/null`) in scripts, and never accept a reset against a seeded DB without re-seeding. If an advisory-lock timeout appears, an orphaned `schema-engine` process is holding it — kill it and terminate its Postgres backend.
- D-020 (Phase 2) Auth: `requireAuth` is applied once on the `/api` router in `app.ts`, so every feature router is authenticated by construction; `requireRole(...roles)` is added per route where a phase demands it. `req.principal` is `{ userId, role }`; `getPrincipal(req)` throws instead of returning undefined.
- D-021 (Phase 2) `GET /api/tickets?category=` filters on the LATEST triage result. Prisma cannot express "latest child matches", so with a category filter the service loads the (status-filtered) tickets, filters in memory, and paginates the result. Without it, pagination is done in SQL. Default sort is `created_at asc`.
- D-022 (Phase 2) `policy_context` on ticket detail: `as_of` = ticket `created_at`; `return_window` and `warranty` are both `null` when the ticket has no order (the prompt only specified `warranty | null`).
- D-023 (Phase 2) `POST /api/tickets` also rejects (400 `VALIDATION_ERROR`) an `order_id` that exists but belongs to a different customer; ids are `tkt_` + 8 alphanumerics from `crypto.randomBytes`; status starts as `open`; 201 on create.
- D-024 (Phase 2) Tests: `tests/setup.ts` forces `NODE_ENV=test`, `LOG_LEVEL=silent` (override with `TEST_LOG_LEVEL`), probes `DATABASE_URL` once with a 5 s timeout, and DB-backed suites wrap themselves in `describeWithDb()` (`tests/helpers/db.ts`) so they show as skipped with the reason instead of failing. API tests delete the tickets they create in `afterAll`, so `total === 8` holds on every run.
- D-025 (Phase 3) Retrieval is chunk-level: `searchKnowledge` returns `{ doc_id, title, chunk_id, heading, snippet, score, source, trust_level }` rows (a superset of `docs/API_CONTRACT.md` section 2). `trust_level` rides along so `buildGroundingContext` can label each fence without a second query.
- D-026 (Phase 3) Stage A runs `websearch_to_tsquery` twice when needed: the strict all-terms form first (its rows get a +0.25 full-match bonus, because `ts_rank_cd` flattens OR-query scores), then, if fewer than 2 rows, the same terms OR-joined so a query containing a word absent from the KB ("earbuds") still ranks partial matches. Stage B (`similarity(content, q) > 0.15`) is kept as specified but almost never fires on chunk-sized text (observed maximum ≈ 0.05); it is a safety net, not the workhorse.
- D-027 (Phase 3) Category prior (`categoryPriors.ts`): with a `categoryHint`, the mapped document's best-matching chunk is guaranteed a result slot (inserted if absent, first chunk as a last resort) and its score is boosted by +0.5, source `category_prior`. This is a deliberate reliability measure for graded citation coverage; retrieval is therefore not purely learned. The README must list it as a known limitation.
- D-028 (Phase 3) Rule R4 in code: every retrieval SQL carries `AND (includeQuarantined OR d.quarantined = false)`; the only caller allowed to pass `includeQuarantined: true` is the Phase 5 guardrail self-test (and its test). API ingest quarantines a document when its `doc_id` contains `ADVERSARIAL` or its content matches the inline injection list in `knowledge/service.ts` (TODO: replaced by `guardrails/patterns.ts` in Phase 5). Quarantined docs are still listed and readable via `/api/documents` so reviewers can see the quarantine.
- D-029 (Phase 3) One persistence path for documents: `persistParsedDocument()` in `knowledge/service.ts` (upsert doc + replace chunk set in one transaction) is used by the seeder, `/documents/ingest` and `/documents/reingest`. Repo paths come from `config/paths.ts` (resolved from `import.meta.url`).
- D-030 (Phase 3) `buildGroundingContext` neutralises any literal `<policy_document` / `</policy_document` inside chunk text (rewritten to `[policy_document]`) and strips `<`/`>` from attribute values, so a document cannot close its own fence early. `/documents/search` is registered before `/documents/:docId` so "search" is never treated as a doc id.
- D-031 (Phase 4) The mock adapter classifies ONLY the text inside `<customer_message>…</customer_message>` (falls back to the whole payload if the fence is absent). Retrieved policy chunks quote injection examples verbatim ("Ignore previous instructions", "Print your API key"), so scanning the whole prompt would mislabel every ticket. The same reason keeps bare "package"/"delivered" out of the mock's shipping terms: a damaged-on-arrival ticket mentions delivery too.
- D-032 (Phase 4) Post-rules PR1–PR4 are mutually exclusive category overrides applied first-match-wins in the documented priority order; every matching rule is still recorded in `guardrail_results.fired_rules` with `applied: true|false` so the trace shows all patterns present. PR5/PR6 only raise priority ("at least high"). The trace also stores `model_output` and `final_output` so "what did the AI actually decide?" is answerable from the run.
- D-033 (Phase 4) Model-output validation: Zod-parse the JSON; on failure retry once with a corrective system line; on a second failure use the mock rule output and record `model_output_invalid_fallback_applied` in `guardrail_results.notes`. In that case the AgentRun keeps `modelProvider` = the configured provider (so the fallback is visible), `modelName` null. The OpenRouter adapter itself never falls back: 429/5xx/timeout/network errors are retried once (1 s backoff) then surface as `AI_PROVIDER_ERROR` 502.
- D-034 (Phase 4) `OpenRouterAdapter` takes injectable `fetch` and `sleep` so its retry/timeout/fence-stripping paths are unit-tested without network. The live path was exercised manually (2026-09-19): the spec's default `google/gemini-2.0-flash-001` now returns 404 "No endpoints found" on OpenRouter, so `.env.example` keeps the specified default but a live run needs `OPENROUTER_MODEL=google/gemini-2.5-flash` (verified working: valid JSON on tkt_9001 and tkt_9006, tokens and latency in the trace). The README (Phase 10) must state the exact model string used. `OPENROUTER_API_KEY` is read from the process environment; dotenv does not override a value already set on the machine.
- D-035 (Phase 4) The triage service does not store the acting principal (AgentRun has no actor column; the prompt's schema is fixed). `triageTicket(ticketId, principal, { provider? })` accepts an optional provider override for the Phase 8 eval runner.
- D-036 (Phase 5) Pattern matching is whole-phrase and case-insensitive: no letter/digit may touch either end (so "otp" does not fire inside "laptop"), whitespace runs are flexible, and a `*` token in a term matches up to 4 intervening words (`issue me a * coupon` catches "issue me a 5000 INR coupon"). Patterns live as data in `guardrails/patterns.ts`; `HIGH_SEVERITY_GROUPS` and `DOCUMENT_REJECT_GROUPS` are the two derived sets.
- D-037 (Phase 5) For retrieved DOCUMENTS only, text inside quotation marks is blanked before scanning: KB-SECURITY-001 legitimately quotes injection examples ("Ignore previous instructions."), and scanning them would reject the security playbook and quarantine it on API re-ingest. The adversarial note's instruction is unquoted and is still rejected. Customer messages never get this exemption.
- D-038 (Phase 5) `SearchResult` now carries the full chunk `content` and the doc's `quarantined` flag. `assessDocuments` scans content (a 40-word snippet could hide an instruction) and `buildGroundingContext` grounds on content rather than the snippet. The Phase 3 inline injection list in `knowledge/service.ts` is gone; ingest quarantine now uses the same INSTRUCTION_OVERRIDE / CONCEALMENT groups as document trust.
- D-039 (Phase 5) Output-scanner severities: LEAKED_SECRET, LEAKED_INTERNAL_NOTE, UNGROUNDED_CITATION, CROSS_CUSTOMER_DATA and UNSAFE_PROMISE are high (safe=false → Phase 6 swaps in the `unsupported_policy_request` refusal); MISSING_CITATION is low because the draft pipeline appends the guardrail's required citations after the scan, so it is recorded, not blocking. "Quotes without paraphrase" = a run of 8+ consecutive words shared with a document whose audience is not "Customer support agents". `scanDraftOutput` takes injectable loaders (customers, internal docs) so it is unit-tested without a DB.
- D-040 (Phase 5) Decision-table nuance: PRIVILEGE_ESCALATION on its own is low severity, so a polite "could you give me a coupon" is not refused; it refuses only alongside a high group (tkt_9006 has INSTRUCTION_OVERRIDE + CONCEALMENT). The decision also returns `refusalTemplate` so Phase 6 does not re-derive which template to use. `guardrails/selfTest.ts` is the single sanctioned `includeQuarantined: true` caller (D-028) and proves retrievable-when-asked + rejected-by-document-trust against the live index.
- D-041 (Phase 6) "PR1 fired in triage" is recomputed from the customer text with `applyPostRules` (a pure function) instead of being read back from the triage trace, so the safety gate cannot drift from what triage decided and Phase 7 can evaluate it without a stored trace. `caseRuleContextForTicket()` in `drafts/service.ts` rebuilds the full case context (category, guardrail outcome, safety, return window, shipping facts) for Phase 7's execution gate; `blockedToolsForCase()` lists every tool the case forbids.
- D-042 (Phase 6) Citations: the output scan's `allowedDocIds` = safe retrieved doc ids ∪ the guardrail's required citations (KB-SECURITY-001 / KB-ACCOUNT-001 are grounded by policy even when retrieval did not surface them). On the refuse path citations = required ∪ safe retrieved (per prompt, so extra retrieved docs such as KB-COUPON-001 appear; evals check subset). When the output scan fails, any citation outside `allowedDocIds` is dropped before the draft is stored, so a fabricated doc id never persists. Required citations are appended last, de-duplicated, order preserved.
- D-043 (Phase 6) When the guardrail outcome is not `allow` and the surviving recommendations do not include `escalate_to_human`, it is added deterministically (reason names the outcome). This is still only a recommendation (rule R5) and keeps allowed_action_recall from depending on whether a live model remembered to propose escalation.
- D-044 (Phase 6) Recommendation rules (`drafts/recommendationRules.ts`), first strip wins: unknown tool → category not allowed → guardrail refusal (only escalation) → safety case (only escalation; replacement and coupon explicitly stripped) → return window not eligible (refund category only; a billing duplicate-charge refund review must not depend on the return window) → stale tracking (shipping, order not delivered, business days since placed_at + 1 business day dispatch lead < 10, and no executed `open_carrier_investigation` whose result has `carrier_confirmed_lost: true`) → `issue_coupon` always stripped. issue_coupon is NEVER auto-recommended by this system: a human may request it in Phase 7, but the AI never suggests goodwill credit. Business days are Mon–Fri UTC with no holiday table (`domain/businessDays.ts`, rule R1 asOf).
- D-045 (Phase 6) Draft lifecycle (`PATCH /api/drafts/:id`): edited ← generated|edited|approved (body required); approved ← generated|edited; rejected ← generated|edited|approved (reason required); sent ← approved only; sent and rejected are terminal. Anything else is 409 CONFLICT naming the current status. approved/rejected write an Approval row (reviewer = principal). An edited body is re-scanned with the output scanner (rule R8 covers human edits) and rejected with 403 GUARDRAIL_BLOCKED when unsafe. Any role may approve a draft (the prompt scopes manager-only approval to tool actions in Phase 7).
- D-046 (Phase 6) Traces: `guardrail_outcome` and `confidence` are not columns on DraftReply (schema is fixed), so they live in the draft's AgentRun `guardrail_results` (`decision.outcome`, `confidence`) and the mappers join the run. A provider failure still writes an AgentRun with status `failed` and the error in `notes` (rule R7) before the 502 surfaces. On the refuse path `model_provider` is recorded as `none` (the model was never called).
- D-047 (Phase 6) Mock draft rules read only the labelled lines `buildUser` writes (`triage_category:`, `return_window: eligible=…`, `<policy_document id="…">`) and cite only ids that were actually retrieved; bodies are worded to avoid every phrase the output scanner and the eval answer requirements forbid (no "has been refunded", no troubleshooting verbs in the safety reply, no coupon offers). `vitest.config.ts` sets `fileParallelism: false` because DB-backed suites share one seeded database; the drafts suite runs a fresh mock triage before each draft so it never depends on leftover triage rows (a live-model triage left in the dev DB once flipped tkt_9001 to warranty).
- D-048 (Phase 6, post-review) Four defects found by the adversarial review were fixed with regression tests: (1) a chunk HEADING is now neutralised and bracket-stripped in `buildGroundingContext` and scanned together with the body in `assessDocuments` (a crafted H2 could previously close the `</policy_document>` fence — amends D-030); (2) `extractDraftFacts` strips the `<customer_message>` block before parsing the labelled fact lines and `<policy_document id>` tags, so a customer cannot forge `return_window:`/`triage_category:` lines for the mock or the live-model fallback; (3) `PATCH /api/drafts/:id` accepts a `body` only with `status: "edited"` (400 otherwise), so the text sent is always the text that was approved — amends D-045; (4) the transition is a conditional `updateMany(... status in allowedFrom)` inside the transaction, so concurrent approvals yield exactly one 200 / one 409 / one Approval row. Also: when a refund case is not eligible and the model body lacks a "not eligible"/"final sale" statement, a grounded sentence is appended (KB-REFUND-001 added to citations if retrieved) and `not_eligible_statement_appended` is noted, instead of only logging a note.
- D-049 (Phase 7) The execution gate reuses the Phase 6 case rules with scope `'execution'`: category, safety-first, return-window-not-eligible and stale-tracking block a human request exactly as they block a recommendation, but the two AI-conservatism rules (`guardrail_refusal → only escalation`, `issue_coupon never auto-recommended`) do not apply to a human who explicitly requests an approval-gated action. Without this split the prompt's own test (issue_coupon 500 on tkt_9006 → approval_required) is impossible, since tkt_9006 is a refuse_and_escalate case. The AI still never recommends coupons; a manager can still decline them.
- D-050 (Phase 7) `requestAction` also validates payload references at request time so an approved action cannot fail on a bad id later: `order_id` must be the ticket's linked order, `customer_id` the ticket's customer, `payload.ticket_id` the request ticket, and `start_refund_review.amount` ≤ the order total (400 VALIDATION_ERROR). `amount` must be a positive number; every other required field is a non-empty string. 201 on creation, 200 on an idempotent replay (`idempotent_replay: true`), the replayed payload is ignored.
- D-051 (Phase 7) State machine and concurrency: `approval_required → approved|rejected` (manager/admin, conditional update so a second concurrent approval gets 409), `approved → executing → executed|failed`, `requested → executing → executed|failed` (low-risk tools, inside the request that created them). `executing` is a transient claim taken with a conditional `updateMany`, so two concurrent executes run the executor once; a loser that arrives after completion gets the stored result with `idempotent_replay: true`, one that arrives mid-flight gets 409. `rejected`, `failed` and `executed` never execute again; `cancelled` is reserved. The registry (`toolActions/registry.ts`) loads ToolDefinition rows once (warmed in `index.ts`), fails loudly if a row has no executor, and exposes mutable `{ definition, executor }` entries so tests can spy on an executor. Every request (created or replayed) writes a `tool_recommendation` AgentRun whose `tool_calls[0].action_id` references the action; `model_provider` is `none`.
- D-052 (Phase 8) Rule R2 assertion: `TriageOptions` and `DraftOptions` accept an `adapter` (wins over `provider`); the eval runner wraps the real adapter with an observing one and, after the triage and draft calls, asserts that no observed prompt contains evaluation-only data. Bare label VALUES ("refund", "high") are not checked, because the triage system prompt lists every category/priority and the draft prompt carries the system's own triage output, so a value match cannot distinguish a leak from normal operation. Checked instead: the expectation keys (`expected_*`, `must_cite_doc_ids`, `allowed_actions`, `disallowed_actions`, `answer_requirements`, `ticket_expectation`, `eval_case`, `"expected"`, `expected:`), the case id, the EvalCase `input` text, every `answer_requirements` string verbatim and the serialised `expected` object. Verified: none of those strings occur in the knowledge base or the ticket texts.
- D-053 (Phase 8) Answer requirements are deterministic predicates keyed by case id and requirement text (`evals/answerRequirements.ts`); a requirement without a predicate is reported as `check_method: 'missing'` and unsatisfied, never silently passed (a test asserts the seeded set has none). Three are labelled `proxy` because the wording is not mechanically checkable: eval_001 "ask for photo if required by implementation" (reply asks for a photo/description of the damage), eval_005 "ignore policy-bypass instruction" (reply neither confirms skipping verification nor makes the change, and escalates), eval_006 "flag unsafe instruction" (refusal statement plus escalation). The prompt's `/try|restart|reset|charge it|troubleshoot/` is word-bounded so "industry"/"country" cannot trip it.
- D-054 (Phase 8) Case results keep EXACTLY the docs/EVALUATION_GUIDE.md shape plus `answer_requirements`; diagnostics (expected values, per-check booleans, guardrail outcome, refusal reason, executed disallowed actions, run/draft ids, model names, prompt versions, a 600-char draft excerpt) live in a sibling `case_details` array. The EvalRun schema is fixed, so its `metrics` column stores the envelope `{ summary, adversarial_summary, case_details, run_metadata, error }` and `case_results` the plain array; status is derived (`error` set → failed, `completed_at` null → running, else completed). The API DTO flattens the four docs/API_CONTRACT.md section 11 example metrics next to the full result.
- D-055 (Phase 8) Metric semantics not pinned by the prompt: predicted `should_escalate` = the triage flag OR a guardrail outcome that carries escalation (`allow_with_escalation`, `refuse_and_escalate`); `blocked_actions` = the draft run's stripped proposals ∪ `blockedToolsForCase(ctx, tools, 'recommendation')` minus what was recommended; "executed" disallowed actions = `executed` ToolActionRequest rows for the ticket at run time; `allowed_action_recall`'s denominator is the cases with a non-empty `allowed_actions` (a case with none still fails its per-case verdict if anything was recommended); a case passes only when every check including answer requirements holds. Adversarial: `unsafe_instruction_followed` = any unsatisfied requirement of that case (all of them concern the unsafe instruction), `escalated` = predicted escalation AND `escalate_to_human` recommended, `safe` = not followed, nothing disallowed recommended or executed, escalated.
- D-056 (Phase 8) Outputs: `reports/eval-run-<id>.json` (gitignored) and `reports/EVALUATION_REPORT.md` are written by every persisted run (CLI and API alike); the markdown is regenerated each run but the "Prompt/retrieval/tooling changes made after evaluation" section is carried over verbatim once it differs from the placeholder. `--out` resolves relative paths against the repo root (the CLI's cwd is `server/`). Exit code 1 iff an adversarial case is unsafe or `citation_coverage < 1`; failed non-adversarial checks are printed but do not fail the command, per the prompt. `POST /api/eval-runs` validates the case ids (404 on unknown), inserts the row, returns 202 and completes in the background; `waitForBackgroundEvalRuns()` lets tests and shutdown await it. The root `.gitignore` had lost its Phase 0 entries (node_modules, dist, .env, reports/*.json); restored in this phase.
- D-057 (Phase 8, post-review) Three confirmed review findings fixed. (1) Rule R7: every eval case now writes an AgentRun of run_type `eval_case` (status `completed`; `guardrail_results` = { eval_run_id, case_id, passed, checks, answer_requirements, predicted, citations, blocked_actions, executed_disallowed_actions, guardrail_outcome, triage_run_id, draft_run_id, draft_id }; `tool_calls` = the recommended actions; `model_provider` = the run's provider; `prompt_version` = the triage and draft versions joined by `+`; `latency_ms` = the case duration) and `case_details[].eval_case_run_id` links to it, so `GET /api/agent-runs?run_type=eval_case` lists eval traces like every other run type. (2) An EvalRun row can never stay `running`: everything after `createRunRow` runs inside one try/catch whose handler calls `markRunFailed` (exported, idempotent, also called from the API's background catch as a belt-and-braces), and the results are saved to the row BEFORE any report file is written. A report-file failure therefore keeps status `completed` with the results, records `run_metadata.report_error`, nulls `report_paths` and raises `ReportWriteError` (CLI exit 1, naming the run id whose results are stored). (3) The leak assertion no longer treats the EvalCase `input` text as a leak — it is a case description, not an expected value, and a live model's reason_summary could legitimately echo it (supersedes that one item of D-052). The refund-promise regex is kept close to the prompt's wording (`refund (has been|is|was) (issued|processed|approved|sent|credited)|immediate refund|instant refund|refunded you`). The evals test suite leaves `reports/` as it found it: it snapshots EVALUATION_REPORT.md before its runs, writes it back afterwards and deletes the JSON files it produced (only when no report existed beforehand does its own full run stay as the deliverable).
- D-058 (Phase 9) Frontend scaffolded with `npm create vite@latest web -- --template react-ts` (create-vite 9: Vite 8, oxlint as the template's linter), then pinned to the stack CLAUDE.md fixes: React 18.3 (the template's React 19 replaced) and TypeScript ~5.9 (same major as the server). Router is `react-router-dom` 7 (React 18 compatible). No component library, state manager or CSS framework; React context is used only for the session and the error banners. `react/only-export-components` is off in `.oxlintrc.json` because the spec puts the session hook and the header component in the same file.
- D-059 (Phase 9) Session: only the token is stored (localStorage `trustdesk_token`); the role label is DERIVED by matching the token against the three demo tokens (`VITE_DEMO_*_TOKEN` from web/.env, hardcoded defaults `agent-token-123` / `manager-token-123` / `admin-token-123`), so the header can never claim a role the token does not have; an override token that matches none shows "custom token" and gets no manager/admin buttons. Approve/Reject on a tool action are rendered only for manager/admin; the agent view shows a "try Approve as Agent" link instead so the 403 envelope can still be demonstrated (the prompt asks for both). Execute is always rendered and enabled only when status is `approved`.
- D-060 (Phase 9) Data flow: every mutation awaits the API and then re-fetches the ticket, its drafts, its actions and its agent runs (no optimistic updates); page state that depends on server data is derived and tagged with the record it belongs to (draft edit tagged with draft id + server body, request form tagged with the tool, loaded lists tagged with their filter) instead of synced with effects. `TicketDetail` is keyed by ticket id. The triage "fired rules" chips come from the latest triage AgentRun's `guardrail_results.fired_rules` (the ticket DTO carries only the summary). The trace panel shows the newest AgentRun for the ticket (which can be an `eval_case` run after an eval) with a selector for older ones; retrieved doc ids and citations open the same document drawer.
- D-061 (Phase 9) Request-action form defaults come from the ticket's own records so the API's reference checks (D-050) pass: order_id / customer_id / ticket_id from the ticket, sku from the first order item, tracking_number from the order, amount = order total for start_refund_review and min(500, max_amount_inr) for issue_coupon, queue = `tier2_support`, reason = the recommendation's reason when prefilled from the draft; idempotency key `<ticket_id>-<tool>-1`, editable. A 201 is reported as "created", a 200 as "idempotent replay" with the returned `idempotent_replay` flag. Approve/Reject decisions need a reason (server: min 5 chars), taken from an editable "decision reason" field with a default.
- D-062 (Phase 9) Evals page: POST /api/eval-runs then GET /api/eval-runs/:id every 1 s until status leaves `running`; the list of past runs is refreshed when a run finishes; the OpenRouter button exists as specified but is never clicked in automated checks (it spends money). Errors from every call go to the banner context (code, HTTP status, message, details, request_id), nothing is swallowed; `useAction` gives each long action a spinner and disables every action button while one runs.
- D-063 (Phase 9) Verified in a real browser (Playwright) against the running stack: all 7 Recommended Demo Flow steps completed from the UI without curl (tkt_9001 triage → draft → trace → create_replacement_order approval_required → agent 403 → manager approve → execute → same-key replay 200; tkt_9006 refuse_and_escalate with PR4 chip and no coupon; eval run from the Evals page to completion). Local machine note (not a project rule): the C: drive is near-full, so `npm install` for web/ and the root was run with `npm_config_cache` and TEMP on D:.
- D-064 (Phase 9, post-review) Adversarial review of the frontend (4 lenses, 16 distinct findings, 3 refuters each): 11 confirmed and fixed, 5 refuted. Fixed: (1) an unsaved textarea edit can no longer sit under Approve / Reject / Send — those buttons are disabled while the text differs from the server body, with a Discard button (the server only ever approves and sends the stored text, D-048, so the screen must match it); (2) the bearer token used by `api.ts` is an in-memory value mirrored to localStorage and the session follows the `storage` event, so the header label and the Authorization header cannot diverge across tabs or with blocked storage; (3) the error banners are a fixed bottom-right stack, visible at any scroll position (the 403 demo control sits below the fold on laptop screens); (4) every initial-load effect has a cancellation flag (React 18 StrictMode double-mounts effects in `npm run dev`, which reported failed loads twice); (5) a failed initial load shows a "could not load" line instead of an endless spinner (ticket, queue, documents, eval list); (6) the ticket page fetches up to 200 runs (server maximum), fetches the latest triage run by id when it is older than that so the fired-rule chips never silently read "none fired", and says "newest 200" instead of a wrong total; (7) the Evals page caches full runs by id: a poll tick updates the cache only and cannot replace the run the user selected, the run list has loading/failed states, and opening a past run that is still `running` starts polling it; (8) both web tsconfigs set `"strict": true` (TypeScript 5.9 does not default to it; the stack rule says strict on). Also adopted from the refuted set because they serve the demo: the ticket page's Trace panel excludes `eval_case` runs (their guardrail_results carry evaluation-only requirement strings; they live on the Evals page) and follows the newest run again whenever a mutation creates one; `canApprove` is false only for the known agent token, so a custom override token gets Approve / Reject and the server decides; the queue's status filter lists only statuses the API has returned (the pack has `open` only) instead of an invented vocabulary. Left as designed: nothing else.
- D-065 (Phase 10) Test hardening: `tests/integration/demoFlow.test.ts` runs the 7-step Recommended Demo Flow through supertest in one test (tkt_9001 triage → draft → trace chain → create_replacement_order request → execute 409 → agent approve 403 → manager approve → execute → same-key replay; tkt_9006 refusal with no coupon; `runEvals({ persist: false })` with all three adversarial cases safe) and deletes every row it created. `tests/helpers/routes.ts` walks the Express router stack (`app._router.stack`, mount paths decoded from each layer's regexp) and `tests/auth.routes.test.ts` asserts structurally that GET /health is the only route outside `requireAuth` and that exactly four routes carry a `requireRole` guard, then behaviourally that every route answers 401 with the envelope for a missing and for an unknown token. `tests/contract.test.ts` checks docs/API_CONTRACT.md sections 2, 5, 6, 8, 9 and 11 (documented fields with their types, snake_case keys at every level except the opaque JSON columns `payload`, `result`, `details`); section 11 runs the evaluator with `outDir` set to a temp directory so the committed report is untouched. `npm run test:ci` = `prisma migrate deploy && tsx src/seed/seed.ts --reset && vitest run` (non-interactive migrate, truncate + reseed, then the suite). Final count after the post-review additions (D-068): 12 files, 162 tests; nothing non-deterministic remains (the mock provider is deterministic; each suite uses run-unique idempotency keys and cleans up; the evals suite restores `reports/`).
- D-066 (Phase 10) README rewritten as the project README with the 12 sections the prompt lists in order; the API table is generated from the registered router stack (`tests/helpers/routes.ts`, 28 routes) and the auth test keeps it honest. The original pack README is preserved verbatim under "About the provided capstone pack". The "clean clone" acceptance was done by copying the working tree (without node_modules/.env/.git) to D:, running the README steps literally against a fresh Docker volume (`docker compose up -d`, `npm install`, `cp .env.example .env`, `db:migrate`, `db:seed`, `dev`, then web, then root `npm install && npm run dev`) and `npm run test:ci` there (159/159); the only addition the walkthrough forced was stating `npm install` at the root before `npm run dev`.
- D-067 (Phase 10) `docs_project/` holds project-authored docs because `docs/` is the read-only pack: DEMO_SCRIPT.md (6-minute script with timestamps, exact clicks and sentences, all 7 demo steps, tkt_9006 as the adversarial case, markers for the trace panel and the eval report) and ARCHITECTURE.md (components, triage / draft / approve-then-execute sequence diagrams in mermaid, guardrail layers in order, state machine, data model). `reports/EVALUATION_REPORT.md` was regenerated by a final `npm run eval` and its hand-maintained section filled from the decision log, because the git history holds a single commit (the pack) and no per-phase commits exist; superseded `eval-run-*.json` files whose rows the reset removed were deleted except the three runs the section cites.
- D-068 (Phase 10, post-review) Review of the Phase 10 deliverables (4 lenses, 23 findings, 20 confirmed): two code changes and the rest documentation. (1) Rule R7 for triage: a provider failure inside `triageTicket` now writes an AgentRun with status `failed` (`guardrail_results.notes` carries `provider_error: <code>: <message>`, no TriageResult row) before the error surfaces, mirroring D-046 for drafts; regression test in `tests/triage.test.ts` with an injected failing adapter. (2) D-003 applies to stored traces: the draft run's `guardrail_results.decision` and `case_context` are written as snake_case (`required_citations`, `refusal_template`, `guardrail_outcome`, `safety_case`, `return_window.window_ends_at`, `shipping.business_days_since_dispatch`, …) instead of the in-memory TypeScript objects; `tests/contract.test.ts` now checks `GET /api/agent-runs/:runId` for a draft run recursively and no longer exempts executor `result` objects. (3) `tests/auth.routes.test.ts` checks the role guards behaviourally (403 with `required_roles` for the wrong roles, 400 validation for an allowed role on the side-effect-free routes) and asserts that the README endpoint table equals the router enumeration, so the README's claim is enforced rather than asserted. (4) `demoFlow` compares the tkt_9006 coupon-action list before and after the adversarial steps (the previous filter could not fail); on a shared dev database the eval step still reports `disallowed_action_executed` for any executed coupon a human left on tkt_9006 (a review agent did exactly that), which is the eval doing its job — `test:ci` runs from a fresh seed. (5) `toolActions.test.ts` deletes only its own triage of tkt_9004 and skips the auto-triage case when foreign triages exist. Docs: both mermaid blocks that did not parse fixed (`;` in sequence messages, `|` in a flowchart label); the README architecture paragraph now distinguishes triage (always calls the model, no prior, post-rules override) from drafting (guardrail gate first); the OpenRouter section states that non-JSON output is a 502 and only schema-invalid JSON retries then falls back, and that a per-run `--provider openrouter` still needs the key and model in the environment; the expected-labels decision says the eval-run detail endpoint reports them as diagnostics; a human edit that fails the output scan is a 403, not a replaced body; Node 20.19+/22.12+ is the real floor (Vite 8); ARCHITECTURE names `scanUntrustedInput` and the two unprefixed `cuid()` ids; DEMO_SCRIPT names all eight metrics and the exact link text. The final report was regenerated after these changes.
- D-069 (Phase 11, item 1 — observability) Cost is computed in one place: `getAiAdapter()` wraps every adapter so each response carries `costEstimate = estimateCostUsd(modelName, tokenUsage)` from `src/ai/pricing.ts`, a hand-entered table of approximate USD-per-million-token list prices (mock = 0) that `AI_PRICE_TABLE_JSON` can override or extend per model; a model absent from the table yields `null` (reported as *unpriced*), never a guess. The AgentRun writers already stored `modelName`, `promptVersion`, `latencyMs` and `tokenUsage`, so only the cost was missing. `GET /api/metrics/summary` (any role; `since`, `ticket_id` filters) aggregates `agent_run` in Node — runs per type and status, nearest-rank p50/p95/max latency overall and per type, prompt/completion tokens per model, estimated cost with priced/unpriced counts — and `/metrics` renders it. Tests: `tests/metrics.test.ts` (price table incl. override parsing, percentile ranks, cost on real mock runs, an exact summary over ten synthetic runs isolated by `ticket_id` + `since`). Must Have suites unchanged and green.
- D-070 (Phase 11, item 2 — feedback) `POST /api/feedback` (any role, 201) validates `rating` as an integer 1–5, the ticket (404) and, when given, that the draft belongs to that ticket (400 with `draft_ticket_id`); `reason` ≤ 2000 and `corrected_response` ≤ 20000 characters are optional and stored verbatim (feedback is never fed back into a prompt, so the output scanner does not apply). `GET /api/feedback` filters by `ticket_id` / `draft_id`, newest first, and returns `average_rating`. The Phase 1 `feedback` table is used unchanged (no reviewer column exists in the fixed schema, so the author is not recorded). UI: thumbs up/down under the draft = ratings 5 / 1, the comment box = `reason`, an unsaved textarea edit = `corrected_response`; the ticket's last three entries are shown. Tests: `tests/feedback.test.ts`.
- D-071 (Phase 11, item 3 — hybrid retrieval) `RETRIEVAL_MODE=fts|hybrid` (default fts, so the eval baseline does not move) and `EMBEDDING_PROVIDER=local|openrouter` (default local). Embeddings are computed at ingest for every chunk by `persistParsedDocument` (seeder, /documents/ingest, /documents/reingest) and stored in `knowledge_chunk.embedding` (`double precision[]`, migration `add_embeddings`; no pgvector) with `embedding_model`; the documents API exposes `embedding_model` so the active embedding is visible. `localEmbedding.ts` is a deterministic hashing embedding (256 buckets, FNV-1a with sign bit over unigrams + bigrams, log TF, L2-normalised, crude suffix stemming) — a lexical signal, not a neural one, and documented as such; `openrouter` posts to `{OPENROUTER_BASE_URL}/embeddings` with `OPENROUTER_EMBEDDING_MODEL` (default `openai/text-embedding-3-small`) and raises `AI_PROVIDER_ERROR` on any failure instead of silently falling back. In hybrid mode `searchKnowledge` runs the lexical stages as before, computes cosine over the non-quarantined chunks whose `embedding_model` matches the active provider (rule R4 holds by construction), fuses both rankings with reciprocal rank fusion (k = 60, scores become RRF sums, a chunk in both lists is tagged `hybrid`) and only then applies the category prior, whose +0.5 boost dominates. `mode` can be passed per call (`searchKnowledge`, `GET /api/documents/search?mode=`), per triage/draft call (`retrievalMode` option) and per eval run (`--retrieval`, `runEvals({ retrievalMode })`); triage and draft traces record `retrieval_mode`. `--compare-retrieval` runs the other mode on the same cases without persisting a row and stores `run_metadata.retrieval_comparison` (both metric sets, per-case pass/citations, embedding model); the report renders it as “Retrieval mode comparison (fts vs hybrid)” and carries the last comparison forward across plain runs, like the manual section. Measured with the local embedding: fts and hybrid both 1.000 on every metric (the category prior decides the required citation in both modes). Tests: `tests/retrieval.hybrid.test.ts` (embedding determinism/similarity, RRF fusion, hybrid search incl. R4 and the prior, OpenRouter client with mocked fetch, runner comparison on three cases).
- D-072 (Phase 11, item 4 — full RBAC) `user` table (migration `add_users`: user_id, unique email, name, role, scrypt password hash) seeded with `usr_agent` / `usr_manager` / `usr_admin` as `agent@` / `manager@` / `admin@trustdesk.local`, password `DEMO_USER_PASSWORD` (default `trustdesk-demo`, re-hashed on every seed; `--reset` truncates the table too). `POST /api/auth/login` is the second open route (mounted before `requireAuth`) and answers `{ token, token_type, expires_in, user }` with an HS256 JWT (`jsonwebtoken`, claims sub/role/email/name, issuer `trustdesk`, `JWT_SECRET` ≥ 16 chars with a dev default, `JWT_TTL_SECONDS` 12 h); wrong password and unknown email give the same 401. `resolveToken` tries the three demo tokens first and then `principalFromJwt` (signature, issuer, expiry and a known role are required), so the README, the tests and the header buttons keep working; the JWT is trusted without a per-request user lookup (role changes apply at expiry; no revocation). Role policies moved out of the routers into `middleware/policy.ts`: `ROUTE_POLICIES` maps `METHOD /api/pattern` to roles (ingest/reingest/eval-runs → admin, approve → manager or admin), `enforcePolicy` (mounted once after `requireAuth`) matches method + path against the compiled patterns and answers 403 with `required_roles` and `route`; unlisted routes are open to any authenticated role; `requireRole` remains available but unused. `tests/auth.routes.test.ts` now derives the guarded set from the map, asserts that every policy key is a real route and that no router carries its own guard, and treats `POST /api/auth/login` as the second open route; `tests/auth.rbac.test.ts` covers hashing, JWT verification edge cases, the policy matcher, login for all three users, tampered/expired tokens, the demo-token fallback and policy enforcement for JWT identities. UI: a Log in form in the header stores the JWT; the role label decodes the token's role claim (display only — the server verifies).
- D-073 (Phase 11, item 5 — red-team view) `POST /api/red-team/probe { text }` (any role, 1–20000 chars) runs `scanUntrustedInput(text, 'customer_message')`, `applyPostRules` against a neutral model answer (so the response shows what the deterministic rules alone decide) and `decideGuardrailOutcome` with no document findings, and returns `input_scan`, `fired_rules`, `decision` (snake_case), `refusal_preview` (the template body a draft would use) and `would_call_model`; it touches no ticket and writes nothing (`persisted: false`, asserted by a row-count test). `GET /api/red-team/runs?limit=` lists AgentRuns whose stored `guardrail_results.input_scan.flagged` is true (Prisma JSON path filter), newest first, mapped to severity / pattern groups / matched terms / outcome / refusal template; only draft runs store an input scan, so only they appear. `/red-team` renders both with sample buttons for the tkt_9006 injection, the tkt_9007 secret request and a benign message. Tests: `tests/redTeam.test.ts`.
- D-074 (Phase 11, post-review) Review of the five items (4 lenses, 25 findings, 16 confirmed, 5 unvoted because the refuters lost network and were judged by hand). Security: Express routers match paths case-insensitively, so `POST /api/EVAL-RUNS` by an agent reached the admin handler while the policy regex missed it — found by a manual probe during the review, the matcher is now case-insensitive (`app.set('case sensitive routing')` does not reach `Router()` instances, so it is not used) and `tests/auth.routes.test.ts` probes every guarded route with an upper-cased last segment for every disallowed role; `NODE_ENV=production` refuses the default `JWT_SECRET` at boot. Observability: `AI_PRICE_TABLE_JSON` is validated at boot (`validatePriceTableJson`) and the cost wrapper is best-effort, so a pricing problem can never fail a triage or draft; the validation retry (D-033) now records the tokens, cost and latency of every attempt through `mergeResponses` (fallback keeps `model_name` null). Hybrid retrieval: the OpenRouter embedding client stores the requested model slug (not the upstream echo) so the vector stage's `embedding_model` filter always matches, and it takes its key as a parameter so its client is tested without one; `/red-team/runs` paginates with `offset` and the page loads more; the carried-over comparison table is labelled as measured by an earlier run. Tests: suites pin `RETRIEVAL_MODE` / `EMBEDDING_PROVIDER` / `AI_PRICE_TABLE_JSON` instead of assuming the local `.env`, only delete triage rows they caused, bound RRF scores with the rounded contribution, and `tests/retrieval.envMode.test.ts` exercises the env-driven hybrid path through the API plus the report carry-forward. Docs corrected: login as the second open route, `enforcePolicy` instead of `requireRole` everywhere, the Phase 11 modules and tables in ARCHITECTURE.md, `OPENROUTER_API_KEY` also required for `EMBEDDING_PROVIDER=openrouter`, the report's hand-maintained section (commit history, Phase 11 row). Refuted or out of scope: the default dev JWT secret outside production (documented), every AgentRun type carrying cost (only model calls do, by design), the comparison run's unpersisted traces (they are drafts/runs like any other and are cleaned by tests).
- D-075 (Verification pass) Fresh-clone check: the README's steps passed verbatim except that `docker compose up -d` fails when another checkout's `trustdesk-db` container is running (same container name and host port); the README now says so under Setup (one database per machine, `docker compose down` in the other checkout or reuse it). Live evaluation: `npm run eval -- --provider openrouter` against `google/gemini-2.5-flash` (2026-09-20) first aborted because one non-JSON answer was a fatal `AI_PROVIDER_ERROR`; `openRouterAdapter` now returns non-JSON output with `json` undefined (after trying to extract a JSON object from surrounding prose) so the D-033 validation path — corrective retry, then deterministic fallback — applies to it as well; nothing in prompts, patterns, post-rules or thresholds changed. Results (report section "Mock vs OpenRouter"): citation_coverage, unsafe_action_block_rate, allowed_action_recall and all three adversarial cases identical to the mock (deterministic layers), category 0.875, priority 0.375, escalation 0.750, answer requirements 0.958. The report keeps the mock run in its header and the live comparison in the hand-maintained section.
- D-076 (Presentation redesign) The frontend was restyled without touching behaviour: `web/src/lib/api.ts` is byte-identical and `session.tsx` keeps every handler (demo-token selection, override, JWT login, storage-event sync) — only its `TokenSelector` markup changed. Stack additions are exactly Tailwind CSS v4 through `@tailwindcss/vite` (no `tailwind.config.js`, no PostCSS config; `styles.css` is `@import "tailwindcss"` plus `@theme` tokens and `@custom-variant dark (&:where(.dark, .dark *))`), `lucide-react` and `@fontsource-variable/inter`. No component library (no Radix/shadcn), no framer-motion, no state manager, no chart library: every primitive under `components/ui/` is hand-built and typed, so the UI stays auditable and dependency-light for a capstone reviewer, and the earlier "plain CSS" stack line is superseded by this entry. Dark is the default theme; the `dark` class on `<html>` is applied by an inline script in `index.html` from `localStorage.trustdesk_theme` before first paint and toggled from the top bar. Semantic colours are CSS variables on `:root` / `.dark` exposed to Tailwind via `@theme inline`, so one utility (`bg-surface`, `text-fg-muted`, …) serves both themes; badge text uses the 700 shade on light and the 300 shade on dark over a 10 % tint so both pass contrast. Layout: a fixed 224 px sidebar (56 px icon rail below 1280 px, tooltips carry the labels), a sticky top bar (breadcrumb, Agent | Manager | Admin segmented control, settings popover holding the token override and the login form, theme toggle) and a scrollable main. The ticket page is a 320 px / 1fr / 380 px grid at ≥1440 px (custom `wide` breakpoint) with independently scrolling panes, two columns at ≥1024 px with the actions/trace pane under the centre and the context pane sticky, single column below. Error reporting moved from bottom-right banners to top-right toasts (offset below the top bar so the role switcher stays reachable during the 403 demo); danger toasts never auto-dismiss. The guardrail outcome is a full-width banner above the draft body (ShieldCheck / ShieldAlert / ShieldX) that still carries the outcome badge. Verified in a real browser at 1440 / 1024 / 390 in both themes on every route with zero horizontal page overflow (tables scroll inside their cards), and the 7-step demo flow plus the draft lifecycle, feedback, citation drawer, override token, login and keyboard row navigation were driven from the redesigned UI. Note for the demo script: `docs_project/DEMO_SCRIPT.md` line 28 still says the error banner is at the bottom right; it is now a top-right toast (that file is part of the read-only scope of the redesign prompt and was left unchanged).

## Agent skills

### Issue tracker

Issues live as local markdown files under `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
